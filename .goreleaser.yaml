builds:
  - env:
      - CGO_ENABLED=0
    main: ./cmd/edge
    id: "edge"
    binary: edge
    goos:
      - linux
      - darwin
      - windows
archives:
  - format_overrides:
      - goos: windows
        format: zip
